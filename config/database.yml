development:
  # adapter: mysql2
  # encoding: utf8
  # database: milk_development
  # host: localhost
  # username: root
  # password:

  adapter: postgresql
  encoding: unicode
  # pool: 4
  database: milk

test:
  adapter: mysql2
  encoding: utf8
  database: milk_test
  host: localhost
  username: root
  password:

production:
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5
  # adapter: mysql2
  # encoding: utf8
  # database: crud-exercise_production
  # host: localhost
  # username: root
  # password: